apiVersion: v1
kind: Service
metadata:
  name: bug-classifier-api-service
spec:
  selector:
    app: bug-classifier-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
